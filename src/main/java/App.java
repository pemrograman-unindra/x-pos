import utils.Color;
import utils.Icon;
import utils.Style;

public class App {
    public static void main(String[] args) {
        Style.printf("X-POX "+Icon.EDIT.toString()+"\n", Color.BLINK_SLOW, Color.HI_CYAN);
    }
}
